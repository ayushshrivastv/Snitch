# Snitch

A company workspace for treasury and onchain payments.

## Getting started

Use Node.js 22.14 or newer, run `npm ci`, then start the application with `npm run dev`.

Keep credentials in an ignored `.env.local` file. `.env.example` contains the configuration names without private values.
